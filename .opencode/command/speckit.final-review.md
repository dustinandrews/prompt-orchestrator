# DEPRECATED: Split into two commands

This command has been split into:

1. **`/speckit.test-review`** — Phase 1: All tests must pass
2. **`/speckit.product-review`** — Phase 2: Golden path validation

Use those instead.
